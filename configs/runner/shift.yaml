# Distribution shift comparison preset.
# Swaps class labels at shift_epoch to test recovery from non-stationarity.
experiment_name: shift_experiment
description: "Distribution shift — label swap at epoch 5"
seeds: [13, 42, 73]
methods:
  - static_bank
  - ed_gb
  - pid_gb
shift_epoch: 5
swap_classes: [0, 2]
freeze_bank: true
shift_test_dataset: true
