# Ablation study preset.
# Varies bank parameters to isolate exposure-debt controller value.
experiment_name: ablation
description: "Ablation over bank capacity, retrieval budget, and warmup"
seeds: [13, 42, 73]
methods: ["ed_gb"]
sweep:
  bank.capacity_per_class: [50, 100, 200, 500]
  method.retrieval_budget: [2, 4, 8, 16]
  method.warmup_steps: [0, 50, 100, 200]
