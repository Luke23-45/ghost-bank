# Core
torch>=2.0.0
pytorch-lightning>=2.0.0
hydra-core>=1.3.0
omegaconf>=2.3.0
numpy>=1.24.0
tqdm>=4.66.0

# Data (optional — HF CDN parquet is much faster than the UofT server)
# pip install ".[data]"  or  pip install huggingface_hub pyarrow pandas

# Dev
pytest>=7.0
pytest-cov>=4.0
